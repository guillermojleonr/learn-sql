# The main pipeline for my_first_declarative_automation_bundle

resources:
  pipelines:
    my_first_declarative_automation_bundle_etl:
      name: my_first_declarative_automation_bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/my_first_declarative_automation_bundle_etl"

      libraries:
        - glob:
            include: ../src/my_first_declarative_automation_bundle_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
